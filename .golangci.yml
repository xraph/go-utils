# golangci-lint configuration for Forge
# Production-ready linting configuration with strict settings

version: "2"

run:
  timeout: 5m
  tests: true
  modules-download-mode: readonly
  allow-parallel-runners: true

output:
  formats:
    text:
      print-linter-name: true
      print-issued-lines: true

linters:
  # Default set of linters.
  # The value can be:
  # - `standard`: https://golangci-lint.run/docs/linters/#enabled-by-default
  # - `all`: enables all linters by default.
  # - `none`: disables all linters by default.
  # - `fast`: enables only linters considered as "fast" (`golangci-lint help linters --json | jq '[ .[] | select(.fast==true) ] | map(.name)'`).
  # Default: standard
  default: none
  # Enable specific linter.
  enable:
    - arangolint
    - asciicheck
    - bidichk
    - bodyclose
    - canonicalheader
    - containedctx
    # - contextcheck
    - copyloopvar
    # - cyclop
    - decorder
    # - depguard
    - dogsled
    # - dupl
    - dupword
    - durationcheck
    - embeddedstructfieldcheck
    - errcheck
    - errchkjson
    - errname
    - errorlint
    - exptostd
    - fatcontext
    - forbidigo
    # - forcetypeassert
    # - funcorder
    # - funlen
    - ginkgolinter
    - gocheckcompilerdirectives
    # - gochecknoglobals
    - gochecknoinits
    - gochecksumtype
    - gocritic
    - godoclint
    - godot
    - goheader
    - gomoddirectives
    - gomodguard
    - goprintffuncname
    - gosec
    - gosmopolitan
    - govet
    - grouper
    # - iface
    - importas
    - inamedparam
    - ineffassign
    # - interfacebloat
    - intrange
    - iotamixing
    # - ireturn
    # - lll
    - loggercheck
    # - maintidx
    - makezero
    - mirror
    - misspell
    # - mnd
    - modernize
    - musttag
    - nakedret
    - nilerr
    - nilnesserr
    - nilnil
    - nlreturn
    - noctx
    - nolintlint
    # - nonamedreturns
    # - nosprintfhostport
    # - paralleltest
    - perfsprint
    - predeclared
    - promlinter
    - protogetter
    - reassign
    - recvcheck
    - rowserrcheck
    - sloglint
    - spancheck
    - sqlclosecheck
    - staticcheck
    - tagalign
    # - tagliatelle
    - testableexamples
    # - testifylint
    # - thelper
    - tparallel
    - unconvert
    # - unqueryvet
    # - unused
    - usestdlibvars
    - usetesting
    - varnamelen
    - wastedassign
    - whitespace
    - wsl_v5
    - zerologlint
  # Disable specific linters.
  disable:
    - asasalint
    - wsl
    - varnamelen
    - exhaustruct
    - wrapcheck
    - exhaustive
    - testpackage
    - revive
    - unused
    - unparam
    - tagliatelle
    - paralleltest
    - prealloc
    - noinlineerr
    - nestif
    - mnd
    - nosprintfhostport
    - lll
    - ireturn
    - iface
    - godox
    - err113
    - interfacebloat
    - funcorder
    - forcetypeassert
    - goconst
    - gocognit
    - gocyclo
    - gochecknoglobals
    - funlen
    - cyclop
    - maintidx
    - dupl
    - contextcheck
    - depguard
    - unqueryvet
    - testifylint
    - thelper
    - nonamedreturns

  # Defines a set of rules to ignore issues.
  # It does not skip the analysis, and so does not ignore "typecheck" errors.
  exclusions:
    # Mode of the generated files analysis.
    #
    # - `strict`: sources are excluded by strictly following the Go generated file convention.
    #    Source files that have lines matching only the following regular expression will be excluded: `^// Code generated .* DO NOT EDIT\.$`
    #    This line must appear before the first non-comment, non-blank text in the file.
    #    https://go.dev/s/generatedcode
    # - `lax`: sources are excluded if they contain lines like `autogenerated file`, `code generated`, `do not edit`, etc.
    # - `disable`: disable the generated files exclusion.
    #
    # Default: strict
    generated: lax
    # Log a warning if an exclusion rule is unused.
    # Default: false
    warn-unused: true
    # Predefined exclusion rules.
    # Default: []
    paths:
      - examples/
      - farp-rust/
    presets:
      - comments
      - std-error-handling
      - common-false-positives
      - legacy
    # Excluding configuration per-path, per-linter, per-text and per-source.
    rules:
      # Exclude some linters from running on tests files.
      - path: _test\.go
        linters:
          - gocyclo
          - errcheck
          - dupl
          - gosec
      # Exclude some `staticcheck` messages.
      - linters:
          - staticcheck
        text: "SA9003:"
      # Exclude gosec G115 (integer overflow) for Field's int64<->uint64/float64
      # bit-pattern round trips. These aren't value-narrowing conversions, they
      # reinterpret the same 64 bits so Field can store any scalar type in one
      # int64 without boxing. log/fields.go was split into log/field.go (the
      # storage side) and the per-format encoders during the logger rewrite.
      - path: log/(field|encoder_json|encoder_pretty)\.go
        linters:
          - gosec
        text: "G115:"
      # Exclude gosec G115 for the two uintptr->int file-descriptor conversions
      # that term.GetSize and term.IsTerminal require. An fd is a small
      # non-negative int on every platform we build for, so there is no
      # meaningful overflow to guard against.
      #
      # This lives here rather than as an inline //nolint because golangci-lint
      # versions disagree about whether G115 applies to uintptr->int at all:
      # 2.10.x reports it, 2.12.x does not. An inline directive therefore fails
      # one version or the other -- unused (nolintlint) on 2.12, or missing
      # (gosec) on 2.10. A config exclusion that matches nothing is only a
      # warning, so this is correct under both.
      - path: log/(config|detect)\.go
        linters:
          - gosec
        text: "G115:"
      # Exclude `lll` issues for long lines with `go:generate`.
      - linters:
          - lll
        source: "^//go:generate "


issues:
  # Maximum issues count per one linter. Set to 0 to disable. Default is 50.
  max-issues-per-linter: 0

  # Maximum count of issues with the same text. Set to 0 to disable. Default is 3.
  max-same-issues: 0

  # Show only new issues created after git revision
  # new: true
  # new-from-rev: HEAD

  # Independently of option `exclude` use or not the default list of exclusions
  # exclude-use-default: false

  # Fix found issues (if it's supported by the linter)
  fix: false

formatters:
  settings:
    goimports:
      local-prefixes: 
        - github.com/xraph/forge

    gofumpt:
      extra-rules: true

severity:
  default: error
  rules:
    - linters:
        - dupl
        - gocyclo
        - lll
      severity: warning

